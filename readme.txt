Ya mira lo estamos usando con xampp esta el blog que ese lo dejo en el usuario del pc la base de datos que la 
importas en el phpmyadmin despues para inciarlo necesitas instalar el composer e intalar el laravel por el cmd con 
este codigo "composer global require laravel/installer" una vez instalado entras a blog desde el cmd y le das 
"php artisan serve" y levantas el laravel